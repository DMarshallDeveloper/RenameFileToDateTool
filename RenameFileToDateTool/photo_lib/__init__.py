"""Shared utilities for the photo-library scripts.

Each module is small and focused; import directly (``from photo_lib.extensions import
IMAGE_EXTENSIONS``) rather than via this package's namespace, so callers explicitly
declare what they depend on.
"""
